# ChronosQ Operations Guide

This guide explains how to start, verify, monitor, back up and stop a
ChronosQ production-style deployment.

## Requirements

Install:

- Docker Desktop or Docker Engine
- Docker Compose
- PowerShell 7 for load testing
- Git

Verify the tools:

```powershell
docker --version
docker compose version
pwsh --version